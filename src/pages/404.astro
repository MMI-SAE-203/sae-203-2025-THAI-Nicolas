---
import Layout from "../layouts/Layout.astro";
import Murderer from "../assets/icones/murderer.webp";
import ImagePB from "../components/ImagePB.astro";

Astro.response.status = 404;
---

<Layout title="Erreur 404" description="La page n'a pas été trouvée" footerMargin="pt-20">
  <main class="flex flex-col items-center justify-center min-h-[70vh] px-4 py-20 text-center bg-black relative overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-full opacity-10 z-0">
    </div>
    
    <div class="absolute right-0 bottom-0 md:right-10 lg:right-20 z-0 opacity-40 md:opacity-30 murderer-container">
      <ImagePB 
        src={Murderer.src} 
        alt="Silhouette effrayante" 
        classImg="w-40 md:w-52 lg:w-64 h-auto murderer-image"
        pictureAttributes={{ loading: "eager" }}
      />
    </div>
    
    <div class="relative z-10 max-w-2xl mx-auto">
      <h1 class="text-9xl md:text-[12rem] font-bold text-orange-500 font-cinzel tracking-widest leading-none">
        404
      </h1>
      
      <h2 class="text-2xl md:text-3xl font-bold text-white font-raleway mt-4 mb-8">
        Cette page a disparu dans les ténèbres...
      </h2>
      
      <p class="text-base md:text-lg text-gray-300 mb-10 font-montserrat">
        La page que vous recherchez n'existe pas ou a été déplacée.
        <br class="hidden md:block" />
        Ne restez pas seul dans le noir trop longtemps...
      </p>
      
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 md:gap-6">
        <a 
          href="/" 
          class="w-full sm:w-auto px-6 py-3 bg-orange-500 hover:bg-orange-600 text-white font-bold uppercase tracking-wide rounded-sm transition-all duration-300 hover:shadow-lg"
        >
          Retour à l'accueil
        </a>
        
        <a 
          href="/films" 
          class="w-full sm:w-auto px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white font-bold uppercase tracking-wide rounded-sm transition-all duration-300 hover:shadow-lg"
        >
          Voir nos films d'horreur
        </a>
      </div>
    </div>
    
    <div class="absolute z-0 w-64 h-64 bg-orange-500/5 rounded-full blur-3xl animate-pulse-slow"></div>
    <div class="absolute z-0 w-96 h-96 bg-orange-600/5 rounded-full blur-3xl animate-pulse-slow" style="animation-delay: 2s;"></div>
  </main>
</Layout>

<style>
  @keyframes pulse-slow {
    0%, 100% {
      transform: translateY(0) scale(1);
      opacity: 0.5;
    }
    50% {
      transform: translateY(-20px) scale(1.1);
      opacity: 0.3;
    }
  }
  .animate-pulse-slow {
    animation: pulse-slow 8s infinite ease-in-out;
  }
  
  @keyframes lurking {
    0% {
      transform: translateX(20px) rotate(1deg);
      opacity: 0.6;
    }
    50% {
      transform: translateX(0) rotate(-1deg);
      opacity: 0.7;
    }
    100% {
      transform: translateX(20px) rotate(1deg);
      opacity: 0.6;
    }
  }
  
  @keyframes floating {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  
  .murderer-container {
    animation: lurking 8s infinite ease-in-out;
    filter: drop-shadow(0 0 10px rgba(0, 0, 0, 0.8));
  }
  
  .murderer-image {
    animation: floating 5s infinite ease-in-out;
    transform-origin: bottom center;
  }
  
  @keyframes flicker {
    0%, 18%, 22%, 25%, 53%, 57%, 100% {
      text-shadow: 0 0 7px #ff6200, 0 0 10px #ff6200, 0 0 21px #ff6200;
    }
    20%, 24%, 55% {
      text-shadow: none;
    }
  }
  
  h1 {
    animation: flicker 5s infinite alternate;
  }
  
  a:hover {
    transform: translateY(-3px);
  }
</style>



